[project]
name = "api-server"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "mangum>=0.19.0",
]

[dependency-groups]
dev = [
    # boto3는 Lambda 런타임에 기본 포함. 로컬 실행 시에만 필요.
    "boto3>=1.34.0",
]
